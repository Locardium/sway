{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities por defecto del spike",
  "windows": ["main"],
  "permissions": ["core:default", "native-audio:default"]
}
