{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Permissions only for Android/iOS (native-audio does not compile on desktop)",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["native-audio:default"]
}
