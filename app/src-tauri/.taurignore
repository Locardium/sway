# The `tauri dev`/`android dev` watcher rebuilds when it sees changes in
# here — without this, each build's own outputs (gen/android/**/build,
# .gradle) look like "changes" and trigger an infinite rebuild+install loop.
**/build/
**/.gradle/
